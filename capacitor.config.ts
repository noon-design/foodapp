
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.food.deliveryapp',
  appName: 'Food Delivery App',
  webDir: 'dist'
};

export default config;
